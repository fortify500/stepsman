title: "STEPSMAN Hello World"
steps:
  - name: "Starting" # mandatory
    description: > # optional
      Execute: echo "hello world" > stepsman_hello.world
    do: # optional
      command: "echo" # test is a command line program which returns exit values based on supplied parameters
      arguments:
        - >
          Hello World
        - ">"
        - "stepsman_hello.world"
  - name: "execute test" # mandatory
    description: > # optional
      check that stepshello.world was created
    do: # optional
      command: "test" # test is a command line program which returns exit values based on supplied parameters
      arguments:
        - "-f"
        - "stepsman_hello.world"
        - "&&"
        - "echo"
        - "$?"
